local validKeys = {
    ["FREE_a3f91c4b8e2d47f0b19c6e5a72d4f8c1"] = true,
    ["FREE_f7b24d9e13ac4f0e8c52b1a6d3e9470f"] = true,
    ["FREE_4c9e81b2f0da47c3a6f5d1e28b7c93fa"] = true,
    ["FREE_91e4b7c2d3a84f1f5b0c2e9a7d4f36b8"] = true,
}

local gui = Instance.new("ScreenGui")
gui.Parent = game.CoreGui
gui.IgnoreGuiInset = true

local bg = Instance.new("Frame")
bg.Parent = gui
bg.Size = UDim2.new(1,0,1,0)
bg.BackgroundColor3 = Color3.fromRGB(15,20,30)

local leftPanel = Instance.new("Frame")
leftPanel.Parent = bg
leftPanel.Size = UDim2.new(0.55,0,1,0)
leftPanel.BackgroundColor3 = Color3.fromRGB(10,15,25)
leftPanel.BorderSizePixel = 0

local title = Instance.new("TextLabel")
title.Parent = leftPanel
title.Position = UDim2.new(0.08,0,0.42,0)
title.Size = UDim2.new(0.7,0,0,50)
title.BackgroundTransparency = 1
title.Text = "xHesiGod has loaded in!"
title.Font = Enum.Font.GothamBold
title.TextColor3 = Color3.fromRGB(255,255,255)
title.TextSize = 28
title.TextXAlignment = Enum.TextXAlignment.Left

local subtitle = Instance.new("TextLabel")
subtitle.Parent = leftPanel
subtitle.Position = UDim2.new(0.08,0,0.48,0)
subtitle.Size = UDim2.new(0.7,0,0,30)
subtitle.BackgroundTransparency = 1
subtitle.Text = "In this track game, everyone's quick."
subtitle.Font = Enum.Font.Gotham
subtitle.TextColor3 = Color3.fromRGB(160,160,160)
subtitle.TextSize = 15
subtitle.TextXAlignment = Enum.TextXAlignment.Left

local barBg = Instance.new("Frame")
barBg.Parent = leftPanel
barBg.Position = UDim2.new(0.08,0,0.55,0)
barBg.Size = UDim2.new(0.45,0,0,12)
barBg.BackgroundColor3 = Color3.fromRGB(30,35,45)
barBg.BorderSizePixel = 0

local bar = Instance.new("Frame")
bar.Parent = barBg
bar.Size = UDim2.new(1,0,1,0)
bar.BackgroundColor3 = Color3.fromRGB(70,140,255)
bar.BorderSizePixel = 0

local keyFrame = Instance.new("Frame")
keyFrame.Parent = bg
keyFrame.Size = UDim2.new(0,340,0,430)
keyFrame.Position = UDim2.new(0.68,0,0.15,0)
keyFrame.BackgroundColor3 = Color3.fromRGB(18,22,30)
keyFrame.BorderSizePixel = 0

Instance.new("UICorner", keyFrame).CornerRadius = UDim.new(0,12)

local header = Instance.new("TextLabel")
header.Parent = keyFrame
header.Size = UDim2.new(1,0,0,50)
header.Position = UDim2.new(0,20,0,15)
header.BackgroundTransparency = 1
header.Text = "Welcome Back!"
header.Font = Enum.Font.GothamBold
header.TextColor3 = Color3.fromRGB(255,255,255)
header.TextSize = 28
header.TextXAlignment = Enum.TextXAlignment.Left

local input = Instance.new("TextBox")
input.Parent = keyFrame
input.Size = UDim2.new(0.82,0,0,45)
input.Position = UDim2.new(0.09,0,0.22,0)
input.PlaceholderText = "KEY_example"
input.Text = ""
input.BackgroundColor3 = Color3.fromRGB(28,32,42)
input.TextColor3 = Color3.fromRGB(255,255,255)
input.Font = Enum.Font.Gotham
input.TextSize = 16

Instance.new("UICorner", input).CornerRadius = UDim.new(0,8)

local function makeButton(text,posY)
    local btn = Instance.new("TextButton")
    btn.Parent = keyFrame
    btn.Size = UDim2.new(0.82,0,0,45)
    btn.Position = UDim2.new(0.09,0,posY,0)
    btn.Text = text
    btn.BackgroundColor3 = Color3.fromRGB(22,26,36)
    btn.TextColor3 = Color3.fromRGB(200,220,255)
    btn.Font = Enum.Font.GothamBold
    btn.TextSize = 18
    btn.BorderColor3 = Color3.fromRGB(70,140,255)
    btn.BorderSizePixel = 1
    Instance.new("UICorner", btn).CornerRadius = UDim.new(0,8)
    return btn
end

local continueBtn = makeButton("Continue",0.36)
local receiveBtn = makeButton("Receive Key",0.50)
local tutorialBtn = makeButton("Tutorial",0.78)

local invalidBox = Instance.new("TextLabel")
invalidBox.Parent = keyFrame
invalidBox.Size = UDim2.new(0.82,0,0,55)
invalidBox.Position = UDim2.new(0.09,0,0.63,0)
invalidBox.BackgroundColor3 = Color3.fromRGB(18,22,30)
invalidBox.BorderColor3 = Color3.fromRGB(70,70,70)
invalidBox.BorderSizePixel = 1
invalidBox.Text = ""
invalidBox.Font = Enum.Font.Gotham
invalidBox.TextColor3 = Color3.fromRGB(255,80,80)
invalidBox.TextSize = 18

Instance.new("UICorner", invalidBox).CornerRadius = UDim.new(0,8)

continueBtn.MouseButton1Click:Connect(function()
    if validKeys[input.Text] then
        invalidBox.TextColor3 = Color3.fromRGB(80,255,120)
        invalidBox.Text = "Key accepted."
        wait(1)
        gui:Destroy()
    else
        invalidBox.TextColor3 = Color3.fromRGB(255,80,80)
        invalidBox.Text = "Key is invalid."
    end
end)

receiveBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://discord.gg/JxjKqZEnSa")
    end
    receiveBtn.Text = "Link Copied"
end)

tutorialBtn.MouseButton1Click:Connect(function()
    if setclipboard then
        setclipboard("https://tut.com/")
    end
    tutorialBtn.Text = "Link Copied"
end)
